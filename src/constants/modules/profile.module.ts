import { Module } from '@/constants/modules/Module';

const profile: Module = {
  name: 'Profile',
  url: '/profile',
  label: 'module.profile.title',
  desc: 'module.profile.description',
};

export const ProfileModule = {
  profile: profile,
};
